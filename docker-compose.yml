version: '3.8'

services:
  client:
    build:
      context: ./deep-presentation-client   # Path to your frontend Dockerfile
    ports:
      - "8051:8051"         # Exposing frontend on port 3000 (change if necessary)
    volumes:
      - ./shared:/app/shared:rw # Mounts shared directory to /app/shared in backend container
    environment:
      - API_URL=http://api:8080
      - VIDEO_STORAGE=/app/shared
  api:
    build:
      context: ./deep-presentation-api    # Path to your backend Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./shared:/app/shared:rw # Mounts shared directory to /app/shared in backend container
    environment:
      - OPENAI_API_KEY=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
      - WHISPERX_API=http://whisperx_api:8081
      - VIDEO_STORAGE=/app/shared
  whisperx_api:
    build:
      context: ./whisperx-fin    # Path to your backend Dockerfile
    ports:
      - "8081:8080"